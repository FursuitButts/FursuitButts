<div class="content">
  <div class="dtext-container">
    <%= format_text(@page.body, allow_color: true) %>
  </div>
  <%= form_tag(discord_post_path) do %>
    <%= submit_tag "Join the server." %>
  <% end %>
</div>

<%= render(partial: "static/wiki_edit_link", locals: { wiki: @page }) %>
<% content_for(:page_title) do %>
  Discord Server
<% end %>
