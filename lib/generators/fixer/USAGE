Description:
    Generates a new fixer for the database

Example:
    bin/rails generate fixer fixer_name

    This will create:
        db/fixes/<id>_fixer_name.rb

    bin/rails generate fixer fixer_name -e

    This will create:
        db/fixes/<id>_1_fixer_name.rb
        db/fixes/<id>_2_fixer_name.rb
