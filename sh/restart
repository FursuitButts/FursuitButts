#!/usr/bin/env bash
SCRIPT_DIR="$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"

cd $SCRIPT_DIR
git pull
start=`date +%s`
docker-compose -f $SCRIPT_DIR/../docker/docker-compose.yml restart
end=`date +%s`
runtime=$((end-start))
echo "Restart took $runtime seconds"
